<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ads="clr-namespace:Mugs.Ads"
             x:Class="Mugs.Views.InmatesPage"
             x:Name="BrowseItemsPage"
             Title="{Binding Title}">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView
            x:Name="ItemsListView"
            Grid.Row="0"
            ItemsSource="{Binding Inmates}"
            HorizontalOptions="FillAndExpand"
            VerticalOptions="FillAndExpand"
            HasUnevenRows="true"
            RefreshCommand="{Binding LoadInmatesCommand}"
            IsPullToRefreshEnabled="true"
            IsRefreshing="{Binding IsBusy, Mode=OneWay}"
            CachingStrategy="RecycleElement"
            ItemSelected="OnItemSelected"
            ItemAppearing="OnItemAppearing">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Image HeightRequest="250" Aspect="AspectFit">
                                <Image.Source>
                                    <UriImageSource
                                        Uri="{Binding ImageUrl}"
                                        CachingEnabled="True"/>
                                </Image.Source>
                            </Image>
                            <Label 
                                Text="{Binding Name}" 
                                HorizontalTextAlignment="Center"
                                LineBreakMode="NoWrap"
                                Style="{DynamicResource ListItemDetailTextStyle}"
                                FontSize="13"/>
                            <Label 
                                Text="{Binding DateOfBooking}" 
                                HorizontalTextAlignment="Center"
                                LineBreakMode="NoWrap"
                                Style="{DynamicResource ListItemDetailTextStyle}"
                                FontSize="13"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ads:AdMobMain BackgroundColor="Transparent" Grid.Row="1" HeightRequest="60" WidthRequest="468"/>
    </Grid>

</ContentPage>